import { ResponseConstantModel } from './response-constant-model';

describe('ResponseConstantModel', () => {
  it('should create an instance', () => {
    expect(new ResponseConstantModel()).toBeTruthy();
  });
});

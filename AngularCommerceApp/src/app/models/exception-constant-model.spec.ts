import { ExceptionConstantModel } from './exception-constant-model';

describe('ExceptionConstantModel', () => {
  it('should create an instance', () => {
    expect(new ExceptionConstantModel()).toBeTruthy();
  });
});

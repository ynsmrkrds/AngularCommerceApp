import { RegisterRequestModel } from './register-request-model';

describe('RegisterRequestModel', () => {
  it('should create an instance', () => {
    expect(new RegisterRequestModel()).toBeTruthy();
  });
});

import { CreateOrderItemModel } from './create-order-item-model';

describe('CreateOrderItemModel', () => {
  it('should create an instance', () => {
    expect(new CreateOrderItemModel()).toBeTruthy();
  });
});

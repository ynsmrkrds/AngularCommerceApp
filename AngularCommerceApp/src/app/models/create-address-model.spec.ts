import { CreateAddressModel } from './create-address-model';

describe('CreateAddressModel', () => {
  it('should create an instance', () => {
    expect(new CreateAddressModel()).toBeTruthy();
  });
});
